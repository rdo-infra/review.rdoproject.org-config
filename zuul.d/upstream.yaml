---
- pipeline:
    name: openstack-check
    description: |
      Newly uploaded patchsets enter this pipeline to receive an
      initial +/-1 Verified vote.
    success-message: Build succeeded (check pipeline).
    failure-message: |
      Build failed (check pipeline).  For information on how to proceed, see
      http://docs.openstack.org/infra/manual/developers.html#automated-testing
    manager: independent
    require:
      rdoproject.org:
        open: True
        current-patchset: True
    trigger:
      opendev.org:
        - event: patchset-created
        - event: change-restored
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(recheck|check-rdo|verify-rdo)
    success:
      # Note: we are using another connection to report with rdo project ci account
      rdoci:
        Verified: 1
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      # Note: we are using another connection to report with rdo project ci account
      rdoci:
        Verified: -1
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-experimental
    description: |
        Upstream patchsets that have to be checked against a set of experimental jobs.
    success-message: Build succeeded (gate pipeline).
    failure-message: |
      Build failed (gate pipeline).  For information on how to proceed, see
      http://docs.openstack.org/infra/manual/developers.html#automated-testing
    manager: independent
    precedence: normal
    require:
      opendev.org:
        open: True
        current-patchset: True
    trigger:
      opendev.org:
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(check rdo experimental)
    success:
      rdoci: {}
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      rdoci: {}
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-regular
    post-review: true
    description: Jobs in this queue are triggered to run every few hours.
    manager: independent
    precedence: normal
    trigger:
      timer:
        - time: '10 6,10,14,18,22 * * *'
    success:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-periodic-master
    post-review: true
    description: Jobs in this queue are triggered to run every few hours.
    manager: independent
    precedence: high
    trigger:
      timer:
        - time: '10 0,12,18 * * *'
    success:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-periodic-latest-released
    post-review: true
    description: Jobs in this queue are triggered to run every few hours.
    manager: independent
    precedence: high
    trigger:
      timer:
        - time: '10 9,15,21 * * *'
    success:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-periodic-24hr
    post-review: true
    description: Jobs in this queue are triggered to run periodically once a day.
    manager: independent
    precedence: normal
    trigger:
      timer:
        - time: '0 4 * * *'
    success:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-periodic-weekend
    post-review: true
    description: Jobs in this queue are triggered to run periodically twice a week - on the weekend.
    manager: independent
    precedence: normal
    trigger:
      timer:
          # https://zuul-ci.org/docs/zuul/admin/drivers/timer.html says
          # The first weekday is Monday.
          #  If Monday is day 1, Saturday and Sunday should be 6,0
        - time: '0 6 * * 6,0'
    success:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-post
    post-review: true
    description: This pipeline runs jobs that operate after each change is merged.
    manager: independent
    precedence: low
    trigger:
      opendev.org:
        - event: ref-updated
          ref: ^refs/heads/.*$
    success:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"

- pipeline:
    name: openstack-check-rdo
    description: |
      Newly uploaded patchsets enter this pipeline to receive an
      initial +/-1 Verified vote.
    success-message: Build succeeded (check pipeline).
    failure-message: |
      Build failed (check pipeline).  For information on how to proceed, see
      http://docs.openstack.org/infra/manual/developers.html#automated-testing
    manager: independent
    require:
      rdoproject.org:
        open: True
        current-patchset: True
    trigger:
      opendev.org:
        - event: patchset-created
        - event: change-restored
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(recheck|check-rdo)
    success:
      # Note: we are using another connection to report with rdo project ci account
      rdoci:
        Verified: 1
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
    failure:
      # Note: we are using another connection to report with rdo project ci account
      rdoci:
        Verified: -1
      sqlreporter:
      mqtt:
        topic: "zuul/{pipeline}/result/{project}/{branch}/{change}"
