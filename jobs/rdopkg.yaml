- job:
    name: 'rdopkg-unittests'
    defaults: global
    builders:
      - prepare-workspace
      - shell: |
          sudo pip install pytest
          cd $ZUUL_PROJECT
          ./run_tests.sh
    triggers:
      - zuul
    node: rdo-centos7-liberty
