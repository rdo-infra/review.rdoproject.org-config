This role has been created to avoid doing changes in the base
job, that can break the NextGen CI jobs.
Files (playbooks/tasks) that are here and are not changed are
just a symlink to playbooks/crc/ files.

This role is used by the `base-future-extracted-crc`.
