pipelines:
  - name: openstack-check
    description: Newly uploaded patchsets enter this pipeline to receive an initial +/-1 Verified vote from Jenkins.
    manager: IndependentPipelineManager
    source: openstack
    precedence: normal
    require:
      open: True
      current-patchset: True
    trigger:
      openstack:
        - event: patchset-created
        - event: change-restored
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(recheck|reverify|check-rdo|verify-rdo)
    success:
      openstack:
        verified: 1
    failure:
      openstack:
        verified: -1
      smtp:
        from: jenkins@review.rdoproject.org
        to: rdo-infra-list@redhat.com
        subject: '[Zuul] Job failed in openstack-check pipeline: {change.project}'

  - name: openstack-check-verified
    description: Patchsets that have been verified from upstream OpenStack (check pipeline).
    manager: IndependentPipelineManager
    source: openstack
    precedence: normal
    require:
      open: True
      current-patchset: True
    trigger:
      openstack:
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(recheck|reverify|check-rdo|verify-rdo)
        - event: comment-added
          approval:
            - verified: 1
          username: ^(jenkins|zuul)$
    failure:
      smtp:
        from: jenkins@review.rdoproject.org
        to: rdo-infra-list@redhat.com
        subject: '[Zuul] Job failed in openstack-check-verified pipeline: {change.project}'

  - name: openstack-manual
    description: Upstream patchsets that have to be checked manually against a set of jobs.
    manager: IndependentPipelineManager
    source: openstack
    precedence: normal
    require:
      open: True
      current-patchset: True
    trigger:
      openstack:
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(check manual)
    success:
      openstack:
        verified: 1
    failure:
      openstack:
        verified: -1
      smtp:
        from: jenkins@review.rdoproject.org
        to: rdo-infra-list@redhat.com
        subject: '[Zuul] Job failed in manual pipeline: {change.project}'

  - name: openstack-periodic-4hr
    description: Jobs in this queue are triggered to run periodically.
    manager: IndependentPipelineManager
    precedence: low
    trigger:
      timer:
        - time: 'H H/4 * * *'
    failure:
      smtp:
        from: jenkins@review.rdoproject.org
        to: rdo-infra-list@redhat.com
        subject: '[Zuul] Job failed in periodic 4hr pipeline: {change.project}'


  - name: openstack-post
    description: This pipeline runs jobs that operate after each change is merged in upstream OpenStack.
    manager: IndependentPipelineManager
    source: openstack
    precedence: low
    trigger:
      openstack:
        - event: ref-updated
          ref: ^(?!refs/).*$
    failure:
      smtp:
        from: jenkins@review.rdoproject.org
        to: rdo-infra-list@redhat.com
        subject: '[Zuul] Job failed in openstack-post pipeline: {change.project}'

projects:
  - name: openstack/python-openstackclient
    openstack-check-verified:
      - DLRN-rpmbuild

  - name: openstack/python-tripleoclient
    openstack-check-verified:
      - DLRN-rpmbuild

  - name: openstack/rpm-packaging
    openstack-check:
      - DLRN-rpmbuild-rpm-packaging

  - name: openstack/releases
    openstack-post:
      - rdo-send-stable-release

# Note (dmsimard): This is a job to ensure tripleo-quickstart commits do not break review.rdo's
# implementation of third party CI.
  - name: openstack/tripleo-quickstart
    openstack-check:
      - gate-tripleo-ci-centos-7-undercloud-oooq-nv

# Note (dmsimard): This is a job to ensure tripleo-quickstart commits do not break review.rdo's
# implementation of third party CI.
  - name: openstack/tripleo-quickstart-extras
    openstack-check:
      - gate-tripleo-ci-centos-7-undercloud-oooq-nv

  - name: openstack-infra/tripleo-ci
    # Note (rlandy): This is a job to ensure openstack-infra/tripleo-ci commits do not break
    # review.rdo's implementation of third party CI
    openstack-check:
      - gate-tripleo-ci-centos-7-undercloud-oooq-nv
    openstack-manual:
      - manual-tripleo-ci-centos-7-ovb-3ctlr_1comp-featureset001
      - manual-tripleo-ci-centos-7-multinode-1ctlr-featureset023
      - manual-tripleo-ci-centos-7-multinode-1ctlr-featureset005
      - manual-tripleo-ci-centos-7-multinode-1ctlr-featureset006
      - manual-tripleo-ci-centos-7-multinode-1ctlr-featureset007
      - manual-tripleo-ci-centos-7-multinode-1ctlr-featureset008
    openstack-periodic-4hr:
      - periodic-tripleo-ci-centos-7-multinode-1ctlr-featureset005
      - periodic-tripleo-ci-centos-7-multinode-1ctlr-featureset006
      - periodic-tripleo-ci-centos-7-multinode-1ctlr-featureset007
      - periodic-tripleo-ci-centos-7-multinode-1ctlr-featureset008
