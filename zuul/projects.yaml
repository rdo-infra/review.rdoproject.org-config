# This file get appended to layout.yaml, thus the lack of
projects:
  - name: ceilometer
    check:
      - tox-validate
  - name: ceilometer-distgit
    check:
      - pkg-validate
  - name: glance
    check:
      - tox-validate
  - name: glance-distgit
    check:
      - pkg-validate
  - name: ironic
    check:
      - tox-validate
  - name: ironic-distgit
    check:
      - pkg-validate
  - name: keystone
    check:
      - tox-validate
  - name: keystone-distgit
    check:
      - pkg-validate
