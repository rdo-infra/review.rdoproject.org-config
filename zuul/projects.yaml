# This file get appended to layout.yaml, thus the lack of
projects:
  - name: gear-dist
    check:
      - pkg-validate
  - name: horizon-dist
    check:
      - pkg-validate
  - name: horizon
    check:
      - tox-validate
