# This file get appended to layout.yaml, thus the lack of
projects:
  - name: ceilometer
    check:
      - tox-validate
    periodic:
      - upstream-update
  - name: ceilometer-distgit
    check:
      - pkg-validate
    periodic:
      - upstream-update

  - name: glance
    check:
      - tox-validate
    periodic:
      - upstream-update

  - name: glance-distgit
    check:
      - pkg-validate
    periodic:
      - upstream-update

  - name: ironic
    check:
      - tox-validate
    periodic:
      - upstream-update
  - name: ironic-distgit
    check:
      - pkg-validate
    periodic:
      - upstream-update

  - name: keystone
    check:
      - tox-validate
    periodic:
      - upstream-update
  - name: keystone-distgit
    check:
      - pkg-validate
    periodic:
      - upstream-update
