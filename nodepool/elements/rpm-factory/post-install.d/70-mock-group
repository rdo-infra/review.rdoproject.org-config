#!/bin/sh -ex
if grep -q sf-ci /etc/passwd; then
    usermod -a -G mock sf-ci
fi
if grep -q zuul /etc/passwd; then
    usermod -a -G mock zuul
fi
if grep -q jenkins /etc/passwd; then
    usermod -a -G mock jenkins
fi
