- hosts: all
  tasks:
    - name: Create SSH private key from secret
      copy:
        content: "{{ tripleo_gerrit_ssh_key.ssh_private_key }}"
        dest: "~/.ssh/id_rsa_tripleo_gerrit"
        mode: 0600

